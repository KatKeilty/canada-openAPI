# Create file: docker-compose.yml

services:
  jupyter:
    build: .
    ports:
      - "8889:8888"
    volumes:
      - ./src:/app/src
      - ./notebooks:/app/notebooks
      - ./data:/app/data
    environment:
      - JUPYTER_ENABLE_LAB=yes