services:
  jupyter:
    build:
      context: .
      dockerfile: docker/jupyter/Dockerfile
    ports:
      - "8889:8888"
    volumes:
      - ./src:/app/src
      - ./notebooks:/app/notebooks
      - ./data:/app/data
      - ./examples:/app/examples
    environment:
      - JUPYTER_ENABLE_LAB=yes
    container_name: canadaopenapi-jupyter